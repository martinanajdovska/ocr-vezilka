# OCR Vezilka — Python dependencies
#
# Phases 1–3 (alignment, error analysis, synthetic noise):
#   stdlib + numpy/pandas/matplotlib/seaborn
# Phase 4 (ByT5 correction models):
#   transformers + torch (+ optional accelerate for multi-GPU device_map)
#
# Python 3.10+ recommended.
#
# Install (Mac / Apple MPS or CPU):
#     python -m pip install -r requirements.txt
#
# Install (Linux + CUDA 12):
#     pip install --index-url https://download.pytorch.org/whl/cu121 torch
#     pip install -r requirements.txt
#
# Install (CPU only):
#     python -m pip install -r requirements.txt
#     (default PyPI torch wheel is CPU; no extra steps)

# --- Phases 1–3: analysis & plotting ---
numpy>=1.26
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13

# --- Phase 4: neural stack (google/byt5-small) ---
# transformers ships the model + ByT5 byte tokenizer; torch is the backend.
# tokenizers is pinned for stable byte-level tokenizer behavior across versions.
# accelerate is optional but enables device_map="auto" on multi-GPU hosts.
transformers>=4.41,<5
torch>=2.2
tokenizers>=0.19
accelerate>=0.30
